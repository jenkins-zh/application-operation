version: "3.7"
services:
  jenkins-wechat:
    image: docker.io/surenpi/jenkins-wechat:aaa2fb4
    restart: on-failure
    ports:
      - "45678:18080"
    volumes:
      - "/var/wechat/config:/config"
    networks:
      - apps
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
networks:
  apps:
